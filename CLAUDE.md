# CLAUDE.md

This repository is an AFP app repo.

Read `AGENTS.md` first and follow it exactly. It declares the iron rules
(never touch the xcodeproj, signing is a no-go zone, verify.sh is the only
oracle), the read order, the verify protocol, and the milestone recording
contract. `AGENTS.md` is the single source of truth; this file only points
to it.
